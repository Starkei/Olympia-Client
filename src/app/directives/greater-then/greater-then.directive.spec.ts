import { GreaterThenDirective } from './greater-then.directive';

describe('GreaterThenDirective', () => {
  it('should create an instance', () => {
    const directive = new GreaterThenDirective();
    expect(directive).toBeTruthy();
  });
});
