import { LowerThenDirective } from './lower-then.directive';

describe('LowerThenDirective', () => {
  it('should create an instance', () => {
    const directive = new LowerThenDirective();
    expect(directive).toBeTruthy();
  });
});
