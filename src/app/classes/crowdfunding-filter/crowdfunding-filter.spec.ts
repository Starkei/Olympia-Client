import { CrowdfundingFilter } from './crowdfunding-filter';

describe('CrowdfundingFilter', () => {
  it('should create an instance', () => {
    expect(new CrowdfundingFilter()).toBeTruthy();
  });
});
